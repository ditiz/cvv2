import styled from "styled-components";

const RightSide = styled.div`
  display: flex;
  flex-flow: column;
  max-height: 100vh;
  overflow-y: scroll;
  margin-left: 2rem;
`;

export default RightSide;
